{
  "name": "points-service",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "npm i && sls deploy",
    "deploy:production": "npm i && sls deploy --stage production"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.38.0"
  }
}
